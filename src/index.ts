// xleffects — pure fseq frame-processing building blocks.
// Transforms operate on plans plus channel buffers; no layout dependency.

export {
    applyDimming,
    type ModelDimPlan,
    type ApplyDimmingOptions,
} from './dimming';
