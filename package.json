{
  "name": "xleffects",
  "version": "0.1.0",
  "description": "Pure fseq frame-processing building blocks (dimming and other channel-data transforms). No layout dependency: operates on plans plus channel buffers.",
  "type": "module",
  "main": "./dist/xleffects.cjs",
  "module": "./dist/xleffects.mjs",
  "types": "./dist/xleffects.d.ts",
  "license": "AGPL-3.0-only",
  "exports": {
    ".": {
      "types": "./dist/xleffects.d.ts",
      "import": "./dist/xleffects.mjs",
      "require": "./dist/xleffects.cjs"
    }
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build:js": "esbuild src/index.ts --bundle --format=esm --platform=browser --target=es2020 --minify --tree-shaking=true --legal-comments=none --outfile=dist/xleffects.mjs",
    "build:js:cjs": "esbuild src/index.ts --bundle --format=cjs --platform=node --target=node18 --minify --tree-shaking=true --legal-comments=none --outfile=dist/xleffects.cjs",
    "build:types": "pnpm exec dts-bundle-generator -o dist/xleffects.d.ts src/index.ts",
    "build": "pnpm build:js && pnpm build:js:cjs && pnpm build:types"
  },
  "devDependencies": {
    "dts-bundle-generator": "^9.5.1",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  }
}
